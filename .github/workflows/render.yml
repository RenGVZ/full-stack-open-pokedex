name: Render Deploy Action

on:
  push:
    branches:
      - render

jobs:
  build_app:
    runs-on: ubuntu-latest
    steps:
      - name: render-branch deploy
        with:
          service-id: ${{ secrets.RENDER_SERVICE_ID }}
          api-key: ${{ secrets.RENDER_API_KEY }}